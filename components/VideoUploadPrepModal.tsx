import type { VideoUploadPrepModalProps } from "./VideoUploadPrepModal.web";

/** Native: no in-app trim UI (picker export preset handles compression). */
export function VideoUploadPrepModal(_props: VideoUploadPrepModalProps) {
  return null;
}
